import { Inter, Titan_One } from "next/font/google";

export const titleFont = Titan_One({weight: '400'});
export const bodyFont = Inter({weight: '700'});
export const bodyFontLight = Inter({weight: '400'});

export const DRAG_TYPE = "ENTRY";